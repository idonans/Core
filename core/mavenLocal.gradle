apply plugin: 'maven'

group = 'com.idonans.core'
version = rootProject.ext.bcfg.ver.versionName

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri("../repositoryLocal"))
        }
    }
}